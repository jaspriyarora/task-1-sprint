const express = require('express');
const app = express();

// Define API endpoints
app.get('/api', (req, res) => {
  res.send('our project is on pace');
});

// Start the server
const port = 3000;
app.listen(port, () => {
  console.log(`Server running at http://localhost:${port}`);
});
